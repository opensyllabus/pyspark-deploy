---

- name: Create cluster
  hosts: localhost
  tasks:
    - terraform:
        project_path: '{{ playbook_dir }}/terraform'
        state: present

- name: Deploy Spark
  hosts: spark
  gather_facts: no
  become: yes
  roles:
    - spark
